# Deploy to Azure Kubernetes Service
# Build and push image to Azure Container Registry; Deploy to Azure Kubernetes Service
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

trigger:
    branches:
      include:
      - main
    paths:
      include:
      - pipelines/mongo-pipeline.yaml

resources:
- repo: self

variables:

  # Container registry service connection established during pipeline creation
  dockerRegistryServiceConnection: 'c6a073b5-1102-4291-9549-3f3c2dd35a58'
  imageRepository: 'mongoimage'
  containerRegistry: 'shoppingacrfinal.azurecr.io'
  tag: '$(Build.BuildId)'
  imagePullSecret: 'shoppingacrfinaldbed-auth'

  # Agent VM image name
  vmImageName: 'ubuntu-latest'


stages:
- stage: Build
  displayName: Build stage
  jobs:
  - job: Build
    displayName: Build
    pool:
      vmImage: $(vmImageName)
    steps:
    - upload: aks
      artifact: aks

- stage: Deploy
  displayName: Deploy stage
  dependsOn: Build
  jobs:
  - deployment: Deploy
    displayName: Deploy
    pool:
      vmImage: $(vmImageName)
    environment: 'sandeepjain101rundevops.default'
    strategy:
      runOnce:
        deploy:
          steps:
          - task: KubernetesManifest@0
            displayName: Deploy to Kubernetes cluster
            inputs:
              action: deploy
              manifests: |
                $(Pipeline.Workspace)/aks/mongo-secret.yaml
                $(Pipeline.Workspace)/aks/mongo-configmap.yaml
                $(Pipeline.Workspace)/aks/mongo.yaml
              imagePullSecrets: |
                mongo-secret

